{
  "case_id": "cause-chain-reversal",
  "size_bucket": "medium",
  "pressure_tags": ["adversarial", "root_cause_reversal", "medium"],
  "source_word_count": 361,
  "intent": {
    "reader": "someone reading this after the fact (postmortem, handoff) who needs to act on the real cause",
    "task": "know which diagnosis turned out to be correct, not just the order theories were proposed in",
    "rationale": "an outline that faithfully reports the initial theory but drops the reversal is actively misleading for this task, even though the initial-theory facts are individually true"
  },
  "facts": [
    {"id": "f1", "text": "the outage started at 14:02", "category": "descriptive", "weight": 1, "precision_sensitive": true, "source_quote": "The checkout outage started at 14:02"},
    {"id": "f2", "text": "the first signal was a wave of database connection errors from the payments service", "category": "descriptive", "weight": 1, "precision_sensitive": false, "source_quote": "the first signal on-call saw was a wave of database connection errors from the payments service"},
    {"id": "f3", "text": "the initial theory was that the connection pool had been exhausted by traffic", "category": "cause_rationale", "weight": 2.5, "precision_sensitive": false, "source_quote": "the initial theory was that the database's connection pool had simply been exhausted by traffic"},
    {"id": "f4", "text": "as a stopgap, the pool size was raised from 50 to 150 at 14:24", "category": "decision", "weight": 3, "precision_sensitive": true, "source_quote": "the connection pool size was raised from 50 to 150 at 14:24"},
    {"id": "f5", "text": "error rates dropped noticeably after the pool increase, which felt like confirmation of the pool-exhaustion theory", "category": "outcome", "weight": 2.5, "precision_sensitive": false, "source_quote": "error rates did drop noticeably afterward, which at the time felt like confirmation that pool exhaustion was in fact the root cause"},
    {"id": "f6", "text": "error rates were not fully back to normal after the pool increase", "category": "negation", "weight": 3, "precision_sensitive": false, "source_quote": "Error rates were not fully back to normal, though"},
    {"id": "f7", "text": "a secondary investigation found one order-history lookup query taking upward of 8 seconds instead of its usual under-100ms", "category": "failure", "weight": 3, "precision_sensitive": true, "source_quote": "one specific query, used by the payments service to look up a customer's order history, was taking upward of eight seconds per call instead of its usual under-100-millisecond time"},
    {"id": "f8", "text": "every slow call held a database connection open for the full duration of the call", "category": "descriptive", "weight": 1, "precision_sensitive": false, "source_quote": "Every one of those slow calls held a database connection open for the full duration"},
    {"id": "f9", "text": "under load, enough connections were tied up simultaneously to starve everything else, producing the pool-exhaustion symptoms", "category": "cause_rationale", "weight": 2.5, "precision_sensitive": false, "source_quote": "under load that was enough connections tied up simultaneously to starve everything else — which is what actually produced the pool-exhaustion symptoms"},
    {"id": "f10", "text": "the slow query was traced to a schema migration that had shipped two days earlier", "category": "cause_rationale", "weight": 2.5, "precision_sensitive": true, "source_quote": "The slow query itself was traced to a schema migration that had shipped two days earlier"},
    {"id": "f11", "text": "the migration had silently dropped an index the query depended on", "category": "failure", "weight": 3, "precision_sensitive": false, "source_quote": "had silently dropped an index the query depended on"},
    {"id": "f12", "text": "the real root cause was the missing index, not the connection pool", "category": "outcome", "weight": 3, "weight_reason": "this is the corrected conclusion the case is testing -- dropping it while keeping the initial theory (f3) would actively mislead a reader acting on the recap", "precision_sensitive": false, "source_quote": "the real root cause was that missing index, not the connection pool"},
    {"id": "f13", "text": "the pool exhaustion was a downstream symptom of the slow query", "category": "cause_rationale", "weight": 2.5, "precision_sensitive": false, "source_quote": "the pool exhaustion was a downstream symptom of the slow query"},
    {"id": "f14", "text": "the slow query was itself a downstream symptom of the migration", "category": "cause_rationale", "weight": 2.5, "precision_sensitive": false, "source_quote": "the slow query was a downstream symptom of the migration"},
    {"id": "f15", "text": "raising the pool size only bought headroom and reduced symptom frequency; it did not fix the query itself", "category": "negation", "weight": 3, "precision_sensitive": false, "source_quote": "it reduced how often the symptom surfaced, but it did nothing to fix the query itself"},
    {"id": "f16", "text": "the same slow-query behavior kept recurring under peak load", "category": "outcome", "weight": 2.5, "precision_sensitive": false, "source_quote": "the same slow-query behavior kept recurring under peak load"},
    {"id": "f17", "text": "the actual fix, landed later that day, was adding the missing index back", "category": "decision", "weight": 3, "precision_sensitive": false, "source_quote": "The actual fix landed later that day: adding the missing index back"},
    {"id": "f18", "text": "the index fix brought the query back under 100 milliseconds and eliminated the connection pileup entirely", "category": "outcome", "weight": 2.5, "precision_sensitive": true, "source_quote": "which brought the query back under 100 milliseconds and eliminated the connection pileup entirely"},
    {"id": "f19", "text": "the pool size was left at 150 afterward since there was no reason to revert it", "category": "outcome", "weight": 2.5, "precision_sensitive": true, "source_quote": "The pool size was left at 150 afterward since there was no reason to revert it"},
    {"id": "f20", "text": "the postmortem was explicit that the pool increase was never the fix; the index was", "category": "negation", "weight": 3, "precision_sensitive": false, "source_quote": "the team's postmortem was explicit that the pool increase was never the fix — the index was"}
  ],
  "relations": [
    {"id": "r1", "type": "causal", "text": "the schema migration (f10) caused the dropped index (f11), which caused the slow query (f7)", "fact_ids": ["f10", "f11", "f7"], "source_quote": "had silently dropped an index the query depended on"},
    {"id": "r2", "type": "causal", "text": "the slow query holding connections open (f7, f8) caused the pool-exhaustion symptoms on-call observed (f9)", "fact_ids": ["f7", "f8", "f9"], "source_quote": "which is what actually produced the pool-exhaustion symptoms on-call had been treating as the root cause"},
    {"id": "r3", "type": "contrast_supersession", "text": "the initial apparent root cause, pool exhaustion (f3), is superseded by the actual root cause, the missing index (f12) — pool exhaustion was only a downstream symptom", "fact_ids": ["f3", "f12", "f13", "f14"], "source_quote": "the real root cause was that missing index, not the connection pool: the pool exhaustion was a downstream symptom of the slow query, and the slow query was a downstream symptom of the migration"},
    {"id": "r4", "type": "causal", "text": "adding the missing index back (f17) caused the query to return under 100ms and eliminated the connection pileup (f18)", "fact_ids": ["f17", "f18"], "source_quote": "which brought the query back under 100 milliseconds and eliminated the connection pileup entirely"},
    {"id": "r5", "type": "contrast_supersession", "text": "the pool increase (f4) reduced symptom frequency but explicitly did not fix the underlying query (f15), and the postmortem later confirmed the pool increase was never the real fix (f20)", "fact_ids": ["f4", "f15", "f20"], "source_quote": "it reduced how often the symptom surfaced, but it did nothing to fix the query itself"},
    {"id": "r6", "type": "temporal_order", "text": "the pool size was raised at 14:24 (f4) before the secondary investigation surfaced the slow query as the real problem (f7)", "fact_ids": ["f4", "f7"], "source_quote": "Error rates were not fully back to normal, though, and a slower secondary investigation kept going in parallel"}
  ],
  "questions": [
    {"id": "q1", "question": "What was the apparent root cause of the outage?", "gold_answer": "Database connection pool exhaustion.", "type": "factual", "fact_ids": ["f3"]},
    {"id": "q2", "question": "What was the actual root cause?", "gold_answer": "A missing database index, dropped by a schema migration two days earlier, which made one query take ~8 seconds and hold connections open.", "type": "factual", "fact_ids": ["f11", "f12"]},
    {"id": "q3", "question": "Was connection pool exhaustion the cause of the outage or a symptom of something else?", "gold_answer": "A symptom — it was downstream of the slow query, which was itself downstream of the migration.", "type": "relational", "fact_ids": ["r3"]},
    {"id": "q4", "question": "Did raising the connection pool size fix the underlying problem?", "gold_answer": "No — it only reduced how often the symptom surfaced; the slow-query behavior kept recurring until the index was added.", "type": "relational", "fact_ids": ["f15", "f16"]},
    {"id": "q5", "question": "What finally fixed the issue?", "gold_answer": "Adding the missing index back.", "type": "factual", "fact_ids": ["f17", "f18"]},
    {"id": "q6", "question": "Did the pool-size increase happen before or after the slow query was identified as the real problem?", "gold_answer": "Before — the pool was raised at 14:24, and the slow query was found later by a secondary investigation running in parallel.", "type": "relational", "fact_ids": ["r6"]},
    {"id": "q7", "question": "Was the connection pool size reverted after the real fix was deployed?", "gold_answer": "No, it was left at 150.", "type": "factual", "fact_ids": ["f19"]},
    {"id": "q8", "question": "How long before the outage had the schema migration that caused it shipped?", "gold_answer": "Two days.", "type": "factual", "fact_ids": ["f10"]}
  ]
}
